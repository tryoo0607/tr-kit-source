mkdir -p "$SB/projects/alpha" "$SB/projects/_docs/alpha/state"
printf '%s\n' "$SB/projects/alpha" > "$SB/.cwd"
for i in $(seq -w 1 100); do printf '# 작업 %s\n\n| 단계 | 수행 |\n' "$i" > "$SB/projects/_docs/alpha/state/$i.md"; done
